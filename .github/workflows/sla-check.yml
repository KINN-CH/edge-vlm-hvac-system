name: SLA Issue Monitoring

on:
  schedule:
    - cron: "*/5 * * * *"  # 5분마다 실행
  workflow_dispatch:

jobs:
  sla-check:
    runs-on: ubuntu-latest

    steps:
      - name: Check issues
        run: |
          echo "🔍 Checking issues without response..."
          echo "⏱ SLA 기준: 24시간"
          echo "📌 현재 Issue 상태 확인 중..."