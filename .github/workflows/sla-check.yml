name: SLA Issue Monitoring

on:
  schedule:
    - cron: "0 * * * *"  # 1시간마다 실행
  workflow_dispatch:     # 수동 실행 버튼 (강추)

jobs:
  sla-check:
    runs-on: ubuntu-latest

    steps:
      - name: SLA Check Start
        run: echo "Checking issues without response..."